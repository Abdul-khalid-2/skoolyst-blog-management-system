-- Module database schema goes here. Keep each module database independent.
--
-- As of Phase 5 (Database & Models), the schema is defined as versioned,
-- trackable migrations in database/migrations/ (run via `php bin/migrate.php`),
-- not as a single dump file. This file is kept only as a historical note —
-- see database/migrations/ for the actual, current schema.
